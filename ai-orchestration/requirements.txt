scikit-learn>=1.4
numpy>=1.26
joblib>=1.3
requests>=2.31
langgraph>=0.2
chromadb>=0.5  # needed transitively: rag/retriever.py imports dataset_tools.chroma_store
pydantic>=2.0  # already a transitive langgraph dependency; pinned explicitly since Week 8's
               # rca/rag/remediation models depend on it directly

# test-only
pytest>=8.0
responses>=0.25
