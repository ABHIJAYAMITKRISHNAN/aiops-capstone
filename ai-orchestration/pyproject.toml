[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-orchestration"
version = "0.1.0"
description = "Week 7: anomaly detection, local LLM interpretation, and LangGraph workflow foundation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
