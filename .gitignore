# Java / Maven
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
.DS_Store
.claude/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Week 5 telemetry - captured data is a generated artifact, not source
telemetry/data/

# Week 6 dataset-tools - the incident dataset and ChromaDB store are both reproducible from
# telemetry/data/ via `python -m dataset_tools.build_dataset` + `chroma_store`, given the fixed
# seed in config.py - generated artifacts, not source
dataset-tools/data/
dataset-tools/chroma/

# Week 7 ai-orchestration - trained models are reproducible from telemetry/data/ +
# dataset-tools/data/memory/ via `python -m ai_orchestration.anomaly.train`
ai-orchestration/models/*/

# Environment / secrets
.env
.env.*
*.pem
*.key

# Node (Streamlit/tooling side artifacts, if any)
node_modules/

# Logs
*.log

# Local data / vector store
chroma_db/
*.sqlite3

# Kubernetes local
.kube/

# OS
Thumbs.db
