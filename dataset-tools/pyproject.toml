[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dataset-tools"
version = "0.1.0"
description = "Week 6: incident dataset construction, Memory/Evaluation split, and ChromaDB retrieval memory"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
